spring:
  data:
    mongodb:
      uri : ${MONGODB_URI}
      database : CaptureTheFlag
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

server:
  port: 8080

logging:
  level:
    org.thymeleaf: DEBUG